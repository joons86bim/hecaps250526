// 스냅샷 존재/일치 확인
const RAW = window.CURRENT_MODEL_URN;
const SAFE = window.CURRENT_MODEL_SAFE_URN;
console.log("[LS] hasRaw/hasSafe", !!localStorage.getItem(`hec:tasks:${RAW}`), !!localStorage.getItem(`hec:tasks:${SAFE}`));
console.log("[mem] __SAVED_TASKS len =", (window.__SAVED_TASKS||[]).length);

// 루트 집계 확인
const D = window.__WBS_DEBUG;
const tree = $.ui.fancytree.getTree("#wbs-tree");
tree.getRootNode().children.forEach(n => console.log("[root counts]", n.title, D.getCounts(n.data.pathKey)));